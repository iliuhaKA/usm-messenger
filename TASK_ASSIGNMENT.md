# USMessenger — Распределение задач по людям

> Документ-компаньон к [TASKS.md](TASKS.md). Описывает, кто какие задачи берёт и почему.
> Номера задач (№1–№22) соответствуют [TASKS.md](TASKS.md).

## Команда

| Человек | Уровень | Роль в проекте |
|---------|---------|----------------|
| **Илья** | Уверенный, есть опыт | Основной разработчик, архитектура, ревью, наставник, сложные/сквозные задачи |
| **Мария** | Почти без опыта ручного кодинга, всё в новинку | Растущий разработчик: фронтовые и изолированные задачи с постепенным усложнением |

> ⚠️ Это **рекомендация**, а не приказ. Мария сама выбирает, что хочет делать — список ниже лишь предлагает разумную траекторию роста. Если она хочет взять что-то «не своё» — отлично, тогда Илья страхует через парное программирование и ревью.

---

## 🎯 Принципы распределения

1. **Марии — задачи с быстрым видимым результатом** (фронт, UI). Видеть результат своего кода на экране мотивирует новичка сильнее всего.
2. **Изоляция важнее размера.** Новичку проще задача, которая трогает 1–2 файла и не ломает остальное, чем «маленькая», но размазанная по всему стеку.
3. **Постепенное усложнение:** сначала чистый фронт → затем фронт + простой бэкенд → затем самостоятельная фича целиком.
4. **Илье — фундамент и риск.** Всё, что про безопасность, шифрование, пагинацию, гонки, real-time протокол и архитектурные решения, на которые потом опираются другие задачи.
5. **Парные задачи** — там, где Марии полезно «вырасти», но в одиночку рискованно: Илья делает бэкенд/каркас, Мария — фронт/UI поверх готового контракта.

---

## 👩‍💻 Мария — рекомендованный трек (от простого к сложному)

### Этап A. «Разогрев»: чистый фронт, 1–2 файла, никакого бэкенда

#### ✅ №10 — Тёмная тема `P2` 🟡 (только фронт)
**Почему ей.** Идеальная первая задача: только фронтенд, мгновенно виден результат, заставляет разобраться в том, как вообще устроены стили проекта (Tailwind, CSS-переменные, структура компонентов) — а это фундамент для всех её будущих задач.
**Что освоит:** Tailwind `darkMode`, CSS-переменные, `localStorage`, состояние в Zustand (`uiStore`).
**Подстраховка Ильи:** показать, как завести `uiStore` и переключение класса на `<html>`; договориться о палитре токенов.

#### ✅ №16 — Медиа-галерея чата `P2` 🟢 (фронт, бэкенд уже готов)
**Почему ей.** Бэкенд-эндпоинт `GET /files/chats/{chatId}` уже существует — Марии не нужно трогать сервер вообще. Она учится работать с готовым API, React Query, рендерить данные. Очень мотивирующе: «я подключилась к настоящему API и показала картинки».
**Что освоит:** вызов API через `api/files.api.ts`, отображение списка, ленивая загрузка картинок, простой lightbox, вкладки в `ChatInfoDrawer`.
**Подстраховка Ильи:** объяснить, как устроен `axios`-клиент и `useQuery`.

---

### Этап B. «Рост»: фронт + первый маленький кусочек бэкенда

#### ✅ №12 — Закрепление сообщений (Pinned messages) `P2` 🟢 (фронт + простой бэк)
**Почему ей.** Первая полноценная сквозная фича, но «маленькая и понятная»: один флаг, один эндпоинт, одна плашка в UI. Илья может отдать ей бэкенд под присмотром — отличный момент впервые написать контроллер/сервис на Spring по образцу уже существующих.
**Что освоит:** добавление поля в сущность + Flyway-миграция (по образцу `V1–V5`), простой эндпоинт `PATCH`, проверка прав (переиспользовать `ensureAdmin`), отрисовка плашки на фронте.
**Подстраховка Ильи:** парное программирование на бэкенд-части; показать, как добавить миграцию и эндпоинт по аналогии с pin/mute чата.

#### ✅ №5 — Редактирование сообщений `P1` 🟢 (фронт + бэк)
**Почему ей.** Дешёвая задача (поле `updatedAt` уже есть), но даёт прочувствовать полный цикл: API → сервис → шифрование → STOMP-событие → UI. Хороший «выпускной» по базовым навыкам.
**Что освоит:** inline-редактор на фронте, повторное использование `MessageCrypto`, обработка STOMP-события `EDITED`.
**Подстраховка Ильи:** Илья к этому моменту уже сделает №2 (удаление) и заведёт единый STOMP-envelope с типами событий — Мария переиспользует этот паттерн.

---

### Этап C. «Вызов»: самостоятельная фича целиком (с ревью, но без надзора)

#### ✅ №9 — Реакции на сообщения (эмодзи) `P1` 🟡 (фронт + бэк, целиком)
**Почему ей.** К этому этапу Мария уже сделала миграцию, эндпоинт и STOMP на предыдущих задачах. Реакции — это «всё то же самое, но самостоятельно и чуть масштабнее»: новая таблица, toggle-логика, агрегация, эмодзи-пикер. Заметная, «вкусная» фича, которой можно гордиться.
**Что освоит:** новая сущность + many-to-one связи, unique-constraint и toggle-логика, агрегация, интеграция эмодзи-пикера, real-time через знакомый STOMP-канал.
**Подстраховка Ильи:** код-ревью архитектуры таблицы реакций до начала; помощь, если запутается в агрегации.

---

### 📈 Итоговая траектория Марии
`Тёмная тема (чистый фронт)` → `Медиа-галерея (фронт + готовый API)` → `Закрепление сообщений (первый бэкенд в паре)` → `Редактирование (полный цикл)` → `Реакции (самостоятельная фича)`.

Каждый шаг добавляет ровно один новый навык, опираясь на предыдущий. К концу она пройдёт весь стек: стили → API → БД/миграции → Spring-эндпоинты → real-time.

---

## 👨‍💻 Илья — основная нагрузка (фундамент, риск, наставничество)

### Критичный фундамент (делать в первую очередь — на это опираются другие)

#### 🔧 №1 — Пагинация сообщений `P0` 🟡
Архитектурная задача: keyset-пагинация, `useInfiniteQuery`, сохранение scroll-anchor, корректная стыковка с real-time. Ошибка здесь ломает всё приложение — нельзя отдавать новичку.

#### 🔧 №2 — Удаление сообщений `P0` 🟡
Здесь Илья проектирует **единый STOMP-envelope** (`CREATED|DELETED|EDITED|...`), которым потом пользуются Мария (edit, реакции) и сам Илья (forward и пр.). Сквозное архитектурное решение.

#### 🔧 №6 — Refresh-токен `P1` 🟡
Безопасность + гонки при параллельных 401 (single-flight refresh) + ротация токенов в Redis. Тонкая тема, цена ошибки — дыра в безопасности.

### Сложные/рискованные фичи

#### 🔧 №4 — Поиск по сообщениям `P1` 🔴
Главная сложность — поиск по **зашифрованным** сообщениям (blind index / trade-off шифрование vs full-text). Требует архитектурного решения.

#### 🔧 №8 — Админ-панель + импорт CSV `P1` 🔴
RBAC, парсинг CSV, генерация/хэширование стартовых паролей, идемпотентность импорта. Завязано на безопасность и выдачу учёток — ядро рабочего процесса.

#### 🔧 №14 — Rate limiting `P2` 🟡
Bucket4j + Redis, защита от брутфорса/флуда. Инфраструктурная задача на стыке безопасности.

### Реплай — берёт Илья, но это кандидат на пару с Марией

#### 🔧 №3 — Ответ на сообщение (reply) `P0` 🟢
Сам по себе несложный (`replyTo` уже есть), но он `P0` и его лучше сделать рано, плюс он трогает скролл-к-оригиналу (общая механика с пином). Илья делает быстро как базу, **либо** отдаёт Марии в пару как тренировку перед №12.

---

## 🤝 Совместные / гибкие задачи

| Задача | Илья | Мария |
|--------|------|-------|
| №7 Папки чатов `P1` 🟡 | Бэкенд (сущности, m2m, API) | Фронт (вкладки в Sidebar, управление папками) |
| №15 Пересылка сообщений `P2` 🟡 | Бэкенд (share GridFS-id, проверки) | Фронт (модалка выбора чата, плашка «переслано») |
| №11 Голосовые сообщения `P2` 🟡 | Помощь с форматами/фолбэками | `MediaRecorder`, UI записи/плеера |
| №20 Read receipts `P3` 🟡 | Вычисление из `lastReadAt` | Иконки-галочки в UI |

Паттерн один и тот же: **Илья фиксирует контракт API/БД → Мария делает фронт поверх готового контракта.** Это и безопасно, и учит её работать «по интерфейсу».

---

## 🧱 Инфраструктура и качество (Илья, по мере готовности фич)

- **№21 CI/CD и деплой** `P3` 🟡 — GitHub Actions, Docker, Railway/Render.
- **№22 Тесты** `P3` 🟡 — каркас тестов Илья; позже Мария может писать простые unit-тесты к своим фичам (отличный способ закрепить понимание).

## 🚀 «На вырост» (пока не берём, но в бэклоге)
№13 Web Push 🔴, №17 WebRTC-звонки 🔴🔴, №18 Треды 🔴, №19 Боты 🟡 — амбициозное, оставляем на потом; почти всё это Илья.

---

## Сводная таблица распределения

| #  | Задача | Приоритет | Кто | Формат |
|----|--------|-----------|-----|--------|
| 10 | Тёмная тема | P2 | **Мария** | самостоятельно |
| 16 | Медиа-галерея чата | P2 | **Мария** | самостоятельно |
| 12 | Закрепление сообщений | P2 | **Мария** | бэк — в паре с Ильёй |
| 5  | Редактирование сообщений | P1 | **Мария** | самостоятельно + ревью |
| 9  | Реакции (эмодзи) | P1 | **Мария** | самостоятельно + ревью |
| 1  | Пагинация сообщений | P0 | **Илья** | — |
| 2  | Удаление сообщений | P0 | **Илья** | задаёт STOMP-envelope |
| 3  | Ответ на сообщение | P0 | **Илья** | (или пара с Марией) |
| 4  | Поиск по сообщениям | P1 | **Илья** | — |
| 6  | Refresh-токен | P1 | **Илья** | — |
| 8  | Админ-панель + CSV | P1 | **Илья** | — |
| 14 | Rate limiting | P2 | **Илья** | — |
| 7  | Папки чатов | P1 | **Оба** | Илья бэк / Мария фронт |
| 15 | Пересылка сообщений | P2 | **Оба** | Илья бэк / Мария фронт |
| 11 | Голосовые сообщения | P2 | **Оба** | Мария фронт / Илья помощь |
| 20 | Read receipts | P3 | **Оба** | Илья бэк / Мария фронт |
| 13 | Web Push | P2 | **Илья** | на вырост |
| 17 | WebRTC-звонки | P3 | **Илья** | на вырост |
| 18 | Треды | P3 | **Илья** | на вырост |
| 19 | Боты/интеграции | P3 | **Илья** | на вырост |
| 21 | CI/CD и деплой | P3 | **Илья** | — |
| 22 | Тесты | P3 | **Оба** | каркас Илья, юниты Мария |

---

## 💡 Советы по наставничеству (для Ильи)

1. **Дай Марии первой выбрать** из её трека (A → B → C) — мотивация от выбора важна.
2. **Контракт вперёд кода.** Для парных задач сначала вместе зафиксируйте формат API (DTO, эндпоинт), потом она спокойно делает фронт.
3. **Ревью как обучение, а не правки.** Объясняй «почему», а не переписывай за неё.
4. **Не отдавай ей `P0`-задачи в одиночку** — цена ошибки в фундаменте слишком высока на старте, фрустрация убьёт мотивацию.
5. **Постепенно поднимай планку:** к №9 (реакции) она должна делать миграцию и эндпоинт уже сама — это и есть рост.
